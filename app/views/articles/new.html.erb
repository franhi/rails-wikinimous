
<h3> New Article </h3>
<%= form_for(@article) do |a| %>
  <%= a.label :title %>
  <%= a.text_field :title %>
  <%= a.label :content %>
  <%= a.text_field :content %>
  <%= a.submit %>

  <%= link_to "Back", articles_path %>
  <% end %>
